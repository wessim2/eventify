apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: eventify-api-hpa
  namespace: eventify-system
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: eventify-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75
